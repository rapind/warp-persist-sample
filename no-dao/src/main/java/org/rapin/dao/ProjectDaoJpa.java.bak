package org.rapin.dao;

import javax.persistence.EntityManager;

import org.rapin.model.Project;

import com.google.inject.Inject;
import com.google.inject.Provider;
import com.google.inject.Singleton;

@Singleton
public class ProjectDaoJpa extends GenericDaoJpa<Project, String> implements
		ProjectDao {

	@Inject
	public ProjectDaoJpa(Provider<EntityManager> entityManager) {
		super(entityManager, Project.class);
	}

}
